# Librerie Python. NB: serve anche "ffmpeg" installato nel sistema
# (non e' un pacchetto pip):
#   - Termux/Android:  pkg install ffmpeg
#   - Debian/Raspberry Pi:  sudo apt install ffmpeg
#   - macOS:  brew install ffmpeg
flask>=3.0
numpy>=1.24
Pillow>=10.0
PyYAML>=6.0
qrcode>=7.4
